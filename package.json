{
  "name": "dev-homoeopathyclinic-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "development",
    "testing"
  ],
  "scripts": {
    "dev": "npm run dev --prefix development",
    "build": "npm run build --prefix development",
    "start": "npm run start --prefix development",
    "lint": "npm run lint --prefix development",
    "test": "npm test --prefix testing",
    "test:e2e": "npm run test:e2e --prefix testing",
    "test:ui": "npm run test:ui --prefix testing"
  }
}
